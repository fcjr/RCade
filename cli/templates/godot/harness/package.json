{
  "name": "beyblade-harness",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild bridge.ts --bundle --outfile=../web-export/rcade-input.js --format=esm --platform=browser",
    "inject": "node inject.js"
  },
  "dependencies": {
    "@rcade/plugin-input-classic": "^0.2.4",
    "@rcade/plugin-input-spinners": "^0.2.3"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.0.0"
  }
}
