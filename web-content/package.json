{
	"name": "web-content",
	"private": true,
	"version": "0.0.1",
	"type": "module",
	"scripts": {
		"dev": "vite dev",
		"preview": "pnpm run --filter @rcade/api build && wrangler dev",
		"prepare": "svelte-kit sync || echo ''",
		"check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
		"check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch",
		"format": "prettier --write .",
		"lint": "prettier --check .",
		"deploy": "wrangler deploy"
	},
	"devDependencies": {
		"@sveltejs/adapter-cloudflare": "^7.2.8",
		"@sveltejs/kit": "^2.60.1",
		"@sveltejs/vite-plugin-svelte": "^6.2.4",
		"@types/pako": "^2.0.3",
		"svelte": "^5.55.9",
		"svelte-check": "^4.3.4",
		"typescript": "^5.9.3",
		"vite": "^7.3.3",
		"wrangler": "^4.93.1"
	},
	"dependencies": {
		"@rcade/api": "workspace:*",
		"@rcade/engine": "workspace:*",
		"@rcade/log": "workspace:*",
		"modern-tar": "^0.7.2",
		"pako": "^2.1.0"
	}
}
