{
  "name": "create-rcade",
  "type": "module",
  "version": "0.0.47",
  "description": "A CLI tool to generate a new rcade project",
  "repository": {
    "type": "git",
    "url": "https://github.com/fcjr/RCade"
  },
  "bin": {
    "create-rcade": "./dist/bin.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "esbuild bin.ts --outdir=dist --platform=node --format=esm --bundle --banner:js=\"import { createRequire as __cr_create_rcade } from 'module'; const require = __cr_create_rcade(import.meta.url);\""
  },
  "dependencies": {
    "commander": "^14.0.2",
    "rcade": "workspace:*"
  },
  "devDependencies": {},
  "peerDependencies": {
    "typescript": "^5"
  }
}
