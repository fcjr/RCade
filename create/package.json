{
  "name": "create-rcade",
  "type": "module",
  "version": "0.0.44",
  "description": "A CLI tool to generate a new rcade project",
  "repository": {
    "type": "git",
    "url": "https://github.com/fcjr/RCade"
  },
  "bin": {
    "create-rcade": "./dist/bin.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "esbuild bin.ts --outdir=dist --platform=node --format=esm --bundle --banner:js=\"import { createRequire as __cr } from 'module'; const require = __cr(import.meta.url);\""
  },
  "dependencies": {
    "commander": "^14.0.2",
    "rcade": "workspace:*"
  },
  "devDependencies": {},
  "peerDependencies": {
    "typescript": "^5"
  }
}
