---
description: Package build and development conventions.
globs: "*.ts, *.tsx, *.html, *.css, *.js, *.jsx, package.json"
alwaysApply: false
---

- Use `pnpm` as the package manager
- Use `esbuild` for bundling TypeScript
- Use `pnpm run <script>` to run scripts
