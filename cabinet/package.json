{
  "name": "@rcade/client",
  "version": "0.1.38",
  "private": true,
  "type": "module",
  "main": "dist/main/main.mjs",
  "scripts": {
    "dev": "bun run --cwd .. build && concurrently \"npm run dev:vite\" \"npm run dev:electron\"",
    "dev:vite": "vite",
    "dev:electron": "electron --trace-warnings .",
    "build": "bun run --cwd .. build && vite build && electron-builder",
    "build:mac": "bun run --cwd .. build && vite build && electron-builder --mac",
    "build:win": "bun run --cwd .. build && vite build && electron-builder --win",
    "build:linux": "bun run --cwd .. build && vite build && electron-builder --linux"
  },
  "dependencies": {
    "@fireworks-js/svelte": "^2.10.8",
    "@hono/node-server": "^1.13.7",
    "@rcade/api": "workspace:*",
    "@rcade/input-classic": "workspace:*",
    "@rcade/input-spinners": "workspace:*",
    "@rcade/sleep": "workspace:*",
    "@rcade/plugin-menu-backend": "workspace:*",
    "@rcade/sdk": "workspace:*",
    "@rcade/sdk-plugin": "workspace:*",
    "hono": "^4.6.14",
    "semver": "^7.7.3",
    "tar": "^7.4.3",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "@rcade/plugin-sleep": "workspace:*",
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "@types/node": "^22.0.0",
    "@types/semver": "^7.7.1",
    "@types/tar": "^6.1.13",
    "concurrently": "^9.0.0",
    "electron": "^39.2.4",
    "electron-builder": "^26.0.12",
    "svelte": "^5.0.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  },
  "build": {
    "npmRebuild": false,
    "appId": "com.frankchiarulli.rcade.client",
    "productName": "rcade",
    "artifactName": "rcade-cabinet-${os}-${arch}.${ext}",
    "files": [
      "dist/**/*",
      "assets/**/*"
    ],
    "directories": {
      "output": "release",
      "buildResources": "assets"
    },
    "mac": {
      "category": "public.app-category.games",
      "target": "zip",
      "icon": "assets/icon.icns"
    },
    "win": {
      "target": "zip",
      "icon": "assets/icon.png"
    },
    "linux": {
      "target": "AppImage",
      "icon": "assets/icon.png"
    }
  }
}
