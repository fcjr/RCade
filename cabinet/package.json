{
  "name": "@rcade/client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/main/main.js",
  "scripts": {
    "dev": "concurrently \"npm run dev:vite\" \"npm run dev:electron\"",
    "dev:vite": "vite",
    "dev:electron": "tsc -p tsconfig.main.json && tsc -p tsconfig.preload.json && electron .",
    "build": "tsc -p tsconfig.main.json && tsc -p tsconfig.preload.json && vite build && electron-builder",
    "build:mac": "tsc -p tsconfig.main.json && tsc -p tsconfig.preload.json && vite build && electron-builder --mac",
    "build:win": "tsc -p tsconfig.main.json && tsc -p tsconfig.preload.json && vite build && electron-builder --win",
    "build:linux": "tsc -p tsconfig.main.json && tsc -p tsconfig.preload.json && vite build && electron-builder --linux"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "@types/node": "^22.0.0",
    "concurrently": "^9.0.0",
    "electron": "^33.0.0",
    "electron-builder": "^25.0.0",
    "svelte": "^5.0.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  },
  "build": {
    "appId": "com.frankchiarulli.rcade.client",
    "productName": "rcade",
    "files": [
      "dist/**/*"
    ],
    "directories": {
      "output": "release"
    },
    "mac": {
      "category": "public.app-category.games"
    },
    "win": {
      "target": "nsis"
    },
    "linux": {
      "target": "AppImage"
    }
  }
}
